# 打卡 · Checkin

一款**默认纯本地运行**的多类型打卡 Android 应用，强调自制力工具属性。所有数据默认存储在设备本地，不上传、不同步；**可选联网增强**（设置页开关，用户自行填入高德 API Key 后才发起逆地理编码请求，默认关闭）。支持多种打卡方式自由组合、负打卡（状态反转）规则、连续打卡抵消机制、多日期排期、随心记与心情日记模式、时间打卡（倒计时 / 正计时 / 暂停续时 / 强制模式），并可将全部数据导出为 JSON 供 PC 端解析。

> 当前版本：**v1.3.6**（versionCode 93） · 最低系统：Android 7.0+（API 24） · 目标 API 34 · 语言：Kotlin

---

## 目录

- [功能特性](#功能特性)
- [打卡方式](#打卡方式)
- [打卡日期排期](#打卡日期排期)
- [核心规则](#核心规则)
- [界面导航](#界面导航)
- [主题系统](#主题系统)
- [日历状态图例](#日历状态图例)
- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [数据存储与导出](#数据存储与导出)
- [构建方法](#构建方法)
- [权限说明](#权限说明)
- [测试](#测试)
- [已知限制与 Roadmap](#已知限制与-roadmap)

---

## 功能特性

### 基础能力

- **默认纯本地运行**：默认无网络请求、无云端同步、无后台常驻 Service，数据 100% 留在设备上。
- **多类型打卡**：普通、拍照、文字、位置、步数、时间、扫码、NFC、语音、自动共 10 种方式，可自由组合。
- **自由组合打卡**：每种方式是独立开关，多选后打卡时逐项完成，全部完成当天变绿。
- **负打卡（状态反转）**：操作 = 破戒失败，无操作 = 默认成功，适合戒手机 / 戒零食等反向自制场景。
- **连续打卡抵消机制**：累积型（A）/ 阈值型（B）/ 周期型（C）三种模式，自动发放 🛡️ 抵消机会补漏签。
- **快捷打卡**：首页专注一个打卡项，长按卡片即可切换；任何时刻唯一（或无），以 ⭐ 标识。
- **打卡日历**：月历视图展示每天状态；点击过去日期查看当天详情（时间、文字、图片、语音）。
- **修改策略**：创建时可选「不可修改」或「N 天可改一次」，锁定规则防作弊。
- **主题系统**：8 套花卉植物风格主题，仅保留打卡按钮色与今天描边色。
- **数据导出**：一键导出全部打卡项、记录、抵消记录为 JSON。
- **状态与文件独立**：打卡状态写入数据库即最终状态，图片 / 语音文件丢失不影响记录。

### 多日期排期

- 创建时可选「每天 / 每周固定几天 / 双休 / 大小周」。
- 无需打卡日以橙色标注、提示「今日无需打卡」，自动视为完成、不中断连续。
- 大小周：大周 = 单休（周六需打卡），小周 = 双休，创建时选本周开头按周交替。

### 组合打卡增强

- **组合完成数**：多选时可设「完成任意 N 项即完成」（0 = 全部），部分完成当天显示 🟡。
- **环形调节**：完成数 1~总数−1 显示数字，总数显示「全部」，+ / − 双向循环不卡边界。
- **已完成项持久化**：组合打卡时已完成的方式当天显示 ✅ 且不可重复打卡（退出重进仍保留）。
- **按钮文案递进**：首次「打卡」→ 后续「继续打卡 (n/总数)」。

### 固定时间段打卡

- 设定时间窗口（如 5:00–8:30），仅窗口内可打卡成功，适合早起场景。
- 与负打卡互斥（圆形单选），不支持跨天窗口，开启后不可选自动打卡。
- 窗口外按钮禁用并提示；**超过结束时间未打卡当天直接红色缺卡**（无需等次日）。

### 无限次数与排序

- **无限次数**：每日次数设为「不限」，打一次立即完成当天状态，按钮显示「继续打卡（N）」，日期下累计 n 次。
- **拖拽排序**：打卡管理页长按卡片右侧 ⋮⋮ 手柄上下拖动；置顶项不可拖，取消置顶回未置顶区最上方。

### 时间打卡

- 原名倒计时打卡，现支持两种模式：
  - **倒计时**：从设定时长倒数，时间到才能完成。
  - **正计时**：从 0 开始，超过设定时长后才能完成；达标后不操作时间继续走，最终按实际用时记录。
- **暂停保存 / 恢复**（仅正计时）：任意时段暂停写入进度，下次打卡或杀后台重进可继续；暂停当天显示 🟡，备注展示已走 / 剩余。
- **强制模式**（v1.3.6，类似番茄钟）：计时期间离开本页（屏幕仍亮）本次计时自动作废（等同放弃，不写记录），需重新开始；**熄屏不算**，解锁回来继续计时；开启后与暂停保存互斥（自动隐藏暂停选项）。
- 负打卡开启时只保留倒计时（隐藏正计时与暂停选项）。

### 随心记与心情日记

- **创建页双 tab**：「✅ 普通打卡 | 📔 日记打卡」分段胶囊切换；日记 tab 为简洁页，无频率 / 排期 / 抵消。
- **随心记**：日记式记录——只记成功、不记缺卡、不设排期；每天可多次记录，有记录即绿色；记录天数「有记录 +1、断签不清零」；拍照 / 文字 / 语音可用，其余方式置灰点击弹颜文字提示。
- **心情日记**：5 档心情（😄 开心 / 🙂 不错 / 😐 一般 / 😟 低落 / 😖 很差）必选，可附文字；日历圆圈 = 当天最后一次带心情记录的心情色，无记录无底色；备注栏当天 ≥2 条带心情且开关开启时画心情折线图；「n 次 / n 天」只计带心情记录。
- **日记项修改策略**：固定 FLEX（随时可改，不锁定）。

### 位置联网增强

- 设置页「联网增强」总开关（默认关）→「定位增强」→ 高德 API Key 输入框三级联动。
- 开启后打卡时把经纬度逆地理编码为地名随记录快照保存，显示「📍 地名 + 距离」。
- 不内置任何 Key；关闭增强或请求失败时回退显示经纬度，历史记录不受影响。

### 交互与视觉

- **文字备注**：备注栏无「文字：」前缀，时间后直接衔接文字；普通打卡 >120 字折叠 5 行 +「…展开」。
- **相机取消直接返回**：拍照取消或启动失败直接返回，不降级相册（保证实时性）。
- **今天外边框**：今天无论是否打卡都显示主题色外边框，有填充时结构为「外框 | 白线 | 内部颜色」。
- **图例两行**：第一行日历圆圈颜色（○今日 / ●已打卡 / ●缺卡 / ●补签 / ●部分完成 / ●无需打卡），第二行备注栏图示。
- **录音按钮配色**：统一青色 #39C5BB + 白字。
- **连续天数颜色**：🔥 连续 N 天用深灰 #4A4A4A（红色易与缺卡撞色）。
- **App 图标**：启动器图标与快捷页空状态占位图为初音未来 Q 版形象。
- **创建页卡片式布局**：名称 / 主题 / 打卡方式 / 记录类型 / 频率与规则等各自独立白卡，卡片间透出页面底色。

---

## 打卡方式

### 普通打卡方式

| 方式 | 说明 | 备注 |
|---|---|---|
| ✅ 普通打卡 | 点一下即完成 | |
| 📷 拍照打卡 | 拍照或从相册选图，自动压缩为 WebP 存储 | 相机取消直接返回，不降级相册 |
| 📝 文字打卡 | 输入文字完成 | 可设最低字数、「不可与上次重复」；备注栏直接衔接文字 |
| 📍 位置打卡 | GPS 定位判定是否在设定范围内 | 支持负打卡（离开设定点才算成功） |
| 👟 步数打卡 | 读取系统计步传感器 | 需身体传感器权限；当前为「开发中」占位 |
| ⏳ 时间打卡 | 倒计时或正计时，达标后可完成；正计时支持暂停续时 | 倒计时：时间到才可完成；正计时：超过目标后可完成，记录实际用时 |
| 🔳 扫码打卡 | 扫创建时生成的二维码，或扫码绑定现有二维码 | 需相机权限 |
| 📡 NFC 打卡 | 碰一碰 NFC 标签，比对标签 ID 后自动完成 | 创建时未绑定标签禁止保存 |
| 🎤 语音打卡 | 录制语音完成，AAC 压缩存储 | 可设最大录制时长；录音按钮 #39C5BB |
| ⚡ 自动打卡 | APP 前台触发自动完成当天打卡 | 每日次数固定为 1；与负打卡 / 固定时间段互斥 |

### 日记打卡方式（日记 tab）

| 方式 | 说明 | 备注 |
|---|---|---|
| 📔 随心记 | 日记式记录，只记成功、可多次、不记缺卡 | 可多选拍照 / 文字 / 语音；每天有一次记录即绿色 |
| 😊 心情日记 | 5 档心情必选 + 可选文字 | 日历取最后一次心情色；可开关备注栏折线图 |

> 多选任意 ≥2 个不冲突的普通方式即成为**组合打卡**：打卡时按方式卡片逐一完成。可设「完成任意 N 项即完成」（部分完成显示 🟡）。组合打卡每日次数固定为 1。

---

## 打卡日期排期

创建打卡项时可选择打卡日期排期。**无需打卡的日子**在日历中以**橙色**标注、提示「今日无需打卡」，且**自动视为完成、不中断连续打卡天数**：

| 模式 | 说明 |
|---|---|
| 每天 | 默认排期，每天都需打卡 |
| 每周固定几天 | 周一~周日 7 个圆形多选，仅选中的日子需打卡 |
| 双休 | 周一~周五需打卡，周六、周日休息 |
| 大小周 | 大周 = 单休（周六需打卡），小周 = 双休；创建时选本周开头，之后按周交替 |

---

## 核心规则

### 负打卡（状态反转）

| 场景 | 显示状态 |
|---|---|
| 某天**执行了**打卡操作 | 该天显示为破戒失败（红色填充） |
| 某天**没有**任何操作 | 该天显示为已打卡（成功） |

负打卡破戒当天连续天数立即归零（不从昨日回溯）。可与所有手动方式叠加，与自动打卡互斥。

### 随心记模式

创建页切到「📔 日记打卡」tab，记录类型选「📔 随心记」：

| 维度 | 行为 |
|---|---|
| 记录语义 | 只记成功，不记缺卡；当天无操作不产生任何失败标记 |
| 每日次数 | 固定「不限」，不可修改 |
| 排期 | 不设排期（每周固定几天 / 双休 / 大小周全部禁用） |
| 连续天数 | 不计算连续天数，改为**记录天数**：有记录 +1，断签不清零 |
| 可记录方式 | 拍照 / 文字 / 语音（单选或多选，可反复多次记录） |
| 禁用方式 | 普通 / 自动 / NFC / 步数 / 时间 / 位置 / 扫码：置灰可点击，弹颜文字提示 |
| 其他禁用 | 负打卡、固定时间段、连续抵消机制（同上） |
| 按钮文案 | 「记录」→ 记录后「继续记录（N）」 |

### 心情日记

创建页日记 tab 记录类型选「😊 心情日记」：

| 维度 | 行为 |
|---|---|
| 必选 | 5 档心情之一（😄🙂😐😟😖） |
| 可选 | 附一段文字（不强制） |
| 日历颜色 | 当天最后一次带心情记录的心情色 |
| 折线图 | 当天 ≥2 条带心情且开关开启时，备注栏第一行画趋势线 |
| 计数 | 「n 次 / n 天」只计带心情的记录（附文字不额外计） |
| 备注栏 | 纯心情记录只显示时间 + emoji，不补「完成心情打卡」 |

### 固定时间段打卡

设定当天内时间窗口（如 05:00–08:30），仅窗口内可打卡成功：

- 窗口内：正常打卡。
- 窗口外（未到 / 已过）：按钮禁用并提示。
- 与负打卡互斥（圆形单选）；与自动打卡互斥。
- 不支持跨天窗口；超过结束时间未打卡当天直接红填充。

### 方式互斥矩阵

- 选「普通打卡」→ 其余 8 个手动方式全部置灰。
- 「自动打卡」与普通 / 拍照 / 文字 / 语音 / 时间 / 扫码 / NFC / 位置 / 步数互斥。
- **负打卡 ↔ 自动打卡**互斥（置灰 + 颜文字提示）。
- **固定时间段 ↔ 自动打卡**互斥。
- 自动打卡开启时每日次数固定为 1 并置灰。

互斥开关保持可点击，点击弹出「温馨提示」说明原因。

### 连续打卡抵消机制

| 模式 | 规则 |
|---|---|
| A 累积型 | 每连续 N 天得 1 次，可累积，断签不清零 |
| B 阈值型 | 每连续达 N 天补足到 k 次（不叠加） |
| C 周期型 | 每完成 N 天周期得 k 次，周期中断签从第 1 天重新计 |

补签计入连续天数；补签当天自动视为完成；补签不产生新抵消机会，里程碑周期从补签当天重新起算。

### 修改策略

- **LOCKED 不可修改**：创建后规则完全锁定（所有规则控件置灰），仅名称 / 图标 / 主题可改。
- **INTERVAL_N N 天可改一次**：距上次修改不足 N 天时修改入口置灰。
- **FLEX 可随时修改**：日记 tab 创建的项固定为 FLEX。
- **一次性迁移工具**（v1.3.6，隐藏入口）：设置页底部**版本号连点 5 次**触发，把历史版本创建的 LOCKED 项一次性迁移为 FLEX（`editPolicy` 改为 FLEX、`lastEditDate`/`editInterval` 清空），仅执行一次，之后入口提示已迁移。迁移项编辑保存后保持 FLEX，不会写回 LOCKED。

### 快捷打卡唯一性

- 任何时刻有且仅有一个（或没有）快捷打卡项。
- 创建第一个打卡项时自动设为快捷；删除后自动切换为排序最靠前的剩余项。
- 切换后首页完全刷新，无残留。

### NFC 打卡

- 创建时开启 NFC → 点「绑定标签」贴近记录 ID；**未绑定禁止保存**。
- 打卡时点 NFC 打卡 → 弹「请贴近标签」→ ID 一致自动完成，不一致提示不匹配。
- 快捷页与打卡操作页均可读卡。

---

## 界面导航

```
┌─────────────────────────────────────────────┐
│               底部导航栏（3 个主页面）           │
│   ┌──────────┐   ┌──────────┐   ┌─────────┐  │
│   │ 打卡管理  │   │ 快捷打卡  │   │  设置   │  │
│   └────┬─────┘   └────┬─────┘   └────┬────┘  │
│        ▼              ▼              ▼        │
│  打卡操作页     打卡操作页(⋮菜单)    数据导出页  │
│  （第二层：隐藏底部导航栏，左上角返回）          │
└─────────────────────────────────────────────┘
```

- **打卡管理**（左）：创建 / 编辑 / 置顶 / 暂停 / 删除；长按卡片弹菜单；点击卡片进入打卡操作页。
- **快捷打卡**（中，默认首页）：一个打卡项的今日状态栏 + 月历 + 记录栏 + 打卡按钮。
- **设置**（右）：数据导出、关于、联网增强开关。
- **打卡操作页**（第二层）：隐藏底部导航，左上角返回；右上角 ⋮ 进详情。

---

## 主题系统

8 套花卉植物风格主题。**所有主题装饰色已取消**，仅保留两处主题色：

| 保留位置 | 说明 |
|---|---|
| 打卡按钮 | 打卡 / 保存等主操作按钮使用主题色 |
| 今天描边 | 日历中「今天」以主题色描边提示 |

| 主题 | emoji | 名称 |
|---|---|---|
| 🌸 樱花粉 | 樱花 | 早起、护肤 |
| 🌻 向日葵 | 向日葵 | 运动、学习 |
| 🍃 薄荷绿 | 薄荷 | 喝水、健康饮食 |
| 💜 薰衣草 | 薰衣草 | 冥想、早睡 |
| 🧡 珊瑚橘 | 珊瑚 | 跑步、健身 |
| 🌊 海洋蓝 | 海洋 | 游泳、阅读 |
| 🌹 玫瑰红 | 玫瑰 | 护肤、穿搭 |
| 🌵 多肉绿 | 多肉 | 养生、散步 |

---

## 日历状态图例

**第一行 · 日历圆圈颜色**（○ 今日未打卡置首位；圆点为带颜色的小号色点）

| 标记 | 含义 |
|---|---|
| ○ 今日未打卡 | 今天还没打卡：仅主题色描边，不填充 |
| ● 已打卡 | 当天已完成打卡（绿色圆） |
| ● 缺卡 | 缺卡日：红色填充 |
| ● 补签 | 通过抵消机会补签成功 |
| ● 部分完成 | 组合打卡有记录未全完成（黄色）；时间打卡暂停进行中（黄色） |
| ● 无需打卡 | 排期休息日（橙色），自动视为完成、不中断连续 |

**第二行 · 备注栏图示**

| 标记 | 含义 |
|---|---|
| ⏳ 今天尚未打卡 | 今天无操作的提示 |
| ✅ 成功 | 正常完成 |
| ↩ 补签 | 补签记录 |
| ⚡ 自动 | 自动打卡记录 |
| ❌ 缺卡 / 破戒 | 缺卡（普通）/ 破戒（负打卡） |
| ⏸ 暂停 | 时间打卡暂停记录（已走 / 目标） |

> 一天多次打卡时，日期下方显示「n次」小标记。

---

## 技术栈

- **语言**：Kotlin 1.9.22（JVM Target 17）
- **UI**：AndroidX 传统 View + Fragment（BottomNavigationView 三页导航 + Activity 详情页）
- **数据库**：Room 2.6.1（KSP 编译），库文件 `checkin6.db`
- **构建**：AGP 8.2.2 / Gradle 8.5
- **最低系统**：Android 7.0（API 24）；目标 API 34
- **网络**：默认纯本地；仅 INTERNET 权限供可选联网增强（默认关闭）
- **后台**：无常驻后台 Service

---

## 项目结构

```
CheckinApp6/
├── app/
│   └── src/main/
│       ├── java/com/zerolab/checkin/
│       │   ├── CheckinApp.kt              # Application（数据库 / 仓库单例）
│       │   ├── data/                      # Room 数据库 / DAO / Entity / Repository
│       │   ├── engine/                    # 方式枚举 / 规则配置 / 状态计算引擎
│       │   ├── theme/ThemeManager.kt      # 8 套花卉主题
│       │   ├── ui/
│       │   │   ├── main/                  # 底部三页导航 + NFC 分发 + 自动打卡
│       │   │   ├── list/                 # 打卡管理页（长按菜单 / 拖拽排序）
│       │   │   ├── quick/                # 首页日历 / 记录栏 / NFC 回调
│       │   │   ├── create/               # 创建 / 编辑（分段胶囊 / 方式开关 / 互斥）
│       │   │   ├── detail/               # 详情 / 打卡操作页
│       │   │   ├── flow/                 # 打卡流程（组合卡片 / 相机 / 计时）
│       │   │   └── settings/             # 设置 / 导出
│       │   └── util/                     # 日期 / 图片 / 音频 / JSON / 联网增强 / 心情折线图
│       └── res/                           # drawable / layout / xml
└── build.gradle / settings.gradle / gradle.properties
```

---

## 数据存储与导出

### Room 表

| 表 | 说明 |
|---|---|
| `checkin_item` | 打卡项（名称、类型、configJson、主题、置顶、修改策略） |
| `quick_checkin_config` | 全局唯一快捷打卡配置 |
| `checkin_record` | 打卡记录（日期、时间戳、状态、是否自动、素材路径、心情值） |
| `offset_credit` | 抵消机会发放 / 使用记录 |
| `auto_checkin_state` | 自动打卡状态 |

### 文件存储

- 图片：`{内部存储}/checkin_photos/{item_id}/{record_id}.webp`（最长边 ≤480px）
- 语音：`{内部存储}/checkin_voices/{item_id}/{record_id}.aac`（16kHz 单声道）
- 打卡状态不依赖文件：文件丢失仅显示占位符，记录保持有效。

### 导出格式

设置 → 数据导出，生成 `checkin_export_yyyyMMdd_HHmmss.json`，包含打卡项、记录、抵消机会、快捷配置等完整数据。

---

## 构建方法

### 环境要求

- JDK 17
- Android SDK（compileSdk 34 / build-tools 34）
- Gradle 8.5

### 构建命令

```bash
# Debug
gradle assembleDebug

# Release
gradle assembleRelease
```

> 本项目未内置 Gradle Wrapper，需使用本机 Gradle 8.5 执行。

### 安装

```bash
adb install -r app-debug.apk
```

---

## 权限说明

所有权限**按需申请**，拒绝仅影响对应功能：

| 权限 | 用途 |
|---|---|
| 相机 CAMERA | 拍照打卡、扫码打卡 |
| 定位 ACCESS_FINE_LOCATION | 位置打卡 |
| 身体传感器 BODY_SENSORS | 步数打卡 |
| NFC | NFC 打卡 |
| 录音 RECORD_AUDIO | 语音打卡 |
| 网络 INTERNET | 可选联网增强（设置页开关默认关） |

不申请存储权限（素材存于应用内部目录）。

---

## 测试

项目在 Android 模拟器（emulator-5558）上进行了多轮实测，覆盖多日连续场景：

- **主流程**：三页导航 / 打卡管理 / 快捷打卡 / 设置导出；卡片直达打卡操作页；详情返回。
- **打卡方式**：普通 / 拍照 / 文字 / 位置 / 步数 / 时间（倒计时 + 正计时 + 暂停）/ 扫码 / NFC / 语音 / 自动，各方式采集与完成链路。
- **组合打卡**：多方式组合、完成数 N/M（环形调节循环边界）、已完成项持久化、按钮文案递进。
- **负打卡**：无操作 = 成功、主动记录 = 破戒；破戒当天连续立即归零；多日连续无操作保持绿色。
- **排期**：每天 / 每周固定几天 / 双休 / 大小周跨周交替；无需打卡日橙色标注且不中断连续。
- **固定时间段**：窗口内可打、窗口外禁用、超结束时间当天直接红填充。
- **时间打卡**：倒计时 / 正计时单选互斥、正计时暂停杀后台续时、达标后继续走、负打卡时只留倒计时。
- **强制模式**（v1.3.6）：创建页开关与暂停保存互斥；计时中按 Home / 切应用（屏幕亮）本次计时自动作废、不写记录；熄屏解锁后弹窗继续计时。
- **LOCKED 迁移**（v1.3.6）：版本号连点 5 次迁移 21 个历史 LOCKED 项为 FLEX；迁移后编辑保存保持 FLEX（回归验证）。
- **随心记**：多方式多选、一天多次记录、记录天数累计、禁用项点击弹颜文字提示、无操作无底色。
- **心情日记**：5 档心情、纯心情 / 心情+文字、日历取最后心情色、折线图开关、备注栏不补"完成心情打卡"。
- **创建页 UI**：普通 / 日记分段胶囊、日记 tab 方式开关内嵌、记录类型互斥、编辑已有日记项记录类型锁定、各区块独立白卡。
- **NFC**：创建绑定 → 快捷页 / 打卡操作页读卡匹配自动完成、不匹配提示。
- **联网增强**：三级开关联动、Key 持久化、失败降级经纬度。
- **连续与抵消**：连续天数累计 / 断签归零 / 补签计入连续 / 里程碑重算。
- **升级兼容**：旧版本数据升级后列表 / 日历 / 记录正常；全新安装空状态引导正常。

> 说明：步数 / 扫码 / NFC / 语音在模拟器上为可交互模拟路径，真实传感器行为需真机验证。联网增强的真实地名返回需真机 + 用户自有 Key 验证，模拟器已验证关闭 / 失败降级路径。

---

## 已知限制与 Roadmap

- [ ] 数据导入（从 JSON 恢复）
- [ ] 主题管理页（目前在创建时选择）
- [ ] 打卡项分组 / 标签
- [ ] 图片 / 语音打包导出
- [ ] 拍照相机比例选择
- [ ] 补充 Gradle Wrapper 与 CI 构建
- [ ] 单元测试 / UI 自动化测试工程化

---

> 封面插画为初音未来（Hatsune Miku）Q 版形象，版权归 Crypton Future Media, Inc. 所有，仅用于本项目的本地演示用途；如用于公开分发，请自行替换为无版权素材。
